{
	"name": "text-api",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"build:dev": "cross-env NODE_ENV=dev && npm run build",
		"build:stage": "cross-env NODE_ENV=stage && npm run build",
		"build:prod": "cross-env NODE_ENV=prod && npm run build",
		"dev": "NODE_ENV=dev nodemon --ext 'ts,json' --ignore dist --exec 'tsc && npm run build && node ./dist/index'"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.19.2",
		"typescript": "^5.4.5"
	},
	"devDependencies": {
		"config": "^3.3.11",
		"@types/config": "^3.3.4",
		"@types/express": "^4.17.21",
		"cross-env": "^7.0.3",
		"nodemon": "^3.1.0"
	}
}
